language: node_js

sudo: required

node_js:
  - "4.0.0"
  - "0.12"
  - "0.10.30"

matrix:
  allow_failures:
  - node_js: "4.0.0"

before_install:
  - "sudo apt-get update"
  - "pushd test; make LIBOTR_DIR=/usr; popd"

script:
  - "make all"
